 1. create enum to define dosa type 
 Podi, masala,onion
2.  create the Dosa Interface to define common method in all dosa type
 spreadFlavour() -- Which is common
3.  create the class of each dosa type ( Implement the classes ) 
 podi 
 masala
 onion
 4. user logger -
 import java.util.logging.Logger;
 private static final Logger logger =Logger.getLogger(PodiDosa.class.getName());
 
 sStep 5 - Create DosaFactory 
    5.1- create switch case base on Dosa type
 switch (dosaTypes) {
		case Podi:
			return new PodiDosa();
		case Masala:
			return new MasalaDosa();
		case Onion:
			return new OnionDosa();
		default:
			
			throw new IllegalArgumentException("Unknown Dosa Types");
  5.2- create prepareDoss()-- common step
  
  step 6 -create the DosaPreHelper class and define common step 
  
  
  // class room exercise 
  //build factory design pattern 
   pre requistie -
   Step 1 - Launch the browser 
   step 2 - Maximized
   step 3- set implicit wait 
   step 4 Load url ( hard code ) 
   
   step 2 to 4 are common 
   
   create enum -> chrome firefox,edge
   create interface  browser - launchBrowser()
   Impl class - (chrome/Edge) and use Logger 
   Build factory class -- call all steptes --> First step ( base on swtich ) _ 3 commonstep
   3 common step should be helper class
   create a class TestBrowser-> do all 4 steps for given browser
   
   
   
   
 